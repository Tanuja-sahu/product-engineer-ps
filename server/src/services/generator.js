export function createGenerator({count=35,delayMs=80,failAfter=null}={}){return async function*(){const words=['Thanks','for','the','message.','This','response','is','streamed','as','small','durable','events','so','a','reconnect','can','continue','safely.'];for(let i=0;i<count;i++){if(failAfter!==null&&i>=failAfter)throw Error('generator_failed');if(delayMs)await new Promise(r=>setTimeout(r,delayMs));yield words[i%words.length]+(i===count-1?'':' ')}}}
